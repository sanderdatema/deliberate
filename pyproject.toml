[project]
name = "deliberators"
version = "0.1.0"
description = "Multi-perspectief AI deliberatie team — Claude Code skill"
requires-python = ">=3.11"
dependencies = [
    "pytest>=9.0.2",
    "pyyaml>=6.0.3",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
